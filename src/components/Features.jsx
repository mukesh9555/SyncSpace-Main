import { motion } from 'framer-motion';
import { BotMessageSquare, Code2, PencilRuler, ShieldCheck, Users, Zap } from 'lucide-react';
import Card from './ui/Card';

const items = [[Users, 'Real-time collaboration', 'See changes instantly and work together without losing context.', '#2563EB'], [PencilRuler, 'Collaborative whiteboard', 'Shape ideas visually with a flexible drawing surface.', '#7C3AED'], [Code2, 'Shared code editor', 'Keep implementation discussions and working notes side by side.', '#10B981'], [BotMessageSquare, 'AI-ready workspace', 'Leave room for future assistants that summarize and accelerate work.', '#7C3AED'], [ShieldCheck, 'Secure by design', 'Protected routes and clear ownership for every workspace.', '#2563EB'], [Zap, 'Fast, focused sync', 'A lightweight workspace designed to keep collaboration responsive.', '#10B981']];
export default function Features() { return <section id="features" className="scroll-mt-24 py-24"><div className="mx-auto mb-14 max-w-2xl text-center"><p className="text-sm font-bold uppercase tracking-widest text-sync-primary">One shared workspace</p><h2 className="mt-3 text-4xl font-bold tracking-tight sm:text-5xl">Everything happens instantly.</h2><p className="mt-4 text-lg text-sync-text-secondary">A calm, capable home for notes, whiteboards, and the work that connects them.</p></div><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{items.map(([Icon, title, description, color]) => <motion.div key={title} whileHover={{ y: -6 }}><Card className="h-full p-7"><span className="mb-6 inline-grid h-12 w-12 place-items-center rounded-xl" style={{ color, backgroundColor: `${color}14` }}><Icon className="h-6 w-6" /></span><h3 className="text-xl font-semibold">{title}</h3><p className="mt-3 leading-7 text-sync-text-secondary">{description}</p></Card></motion.div>)}</div></section>; }
